// theme.js

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976D2", // Adjust the color based on your preference
    },
    secondary: {
      main: "#FF4081", // Adjust the color based on your preference
    },
  },
  typography: {
    fontFamily: "Arial, sans-serif", // Adjust the font family based on your preference
  },
});

export default theme;
